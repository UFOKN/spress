# encode.sf <- function(x, ..., n = 4L) {
#     sf_tbl <- .pull_coords(x)
# }

# encode.sfc <- function(x, ..., n = 4L) {
#     sf_tbl <- .pull_coords(x)
# 
#     if (inherits(x, "POINT")) {
# 
#     } else {
#         
#     }
# }

#' @rdname encode
#' @export
# encode.sfg <- function(x, ..., n = 4L) {
#     sf_tbl <- .pull_coords(x)
#     # sf_tbl <- encode(sf_tbl, coords = c("x", "y"), n = n, attach = TRUE)
# }